class VigenereCipheringMachine {
  constructor(parametr){

  }
  encrypt(message, key) {
    if (message && key) {

    } else {
      throw new Error();
    }
  }    
  decrypt(message, key) {
    if (message && key) {

    } else {
      throw new Error();
    }
  }

}


module.exports = VigenereCipheringMachine;
